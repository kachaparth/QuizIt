version: '3.8'

services:
  quizit-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quizit-backend
    ports:
      - "8080:8080"
    restart: always
    env_file:
      - .env